<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>PCOS Lifestyle Coach</title>
  <link rel="stylesheet" href="<%= basePath %>/styles.css" />
</head>
<body>
<header>
  <div class="container nav">
    <h1><a href="<%= basePath %>/">PCOS Lifestyle Coach</a></h1>
    <nav>
      <a href="<%= basePath %>/">Home</a>
      <a href="<%= basePath %>/about">About</a>
      <a href="<%= basePath %>/recipes">Recipes</a>

      <% if (currentUser) { %>
        <a href="<%= basePath %>/daily-check-in">Daily Check-In</a>
        <span class="nav-user">Hi, <%= currentUser.display_name %></span>
        <a href="<%= basePath %>/logout">Logout</a>
      <% } else { %>
        <a href="<%= basePath %>/register">Register</a>
        <a href="<%= basePath %>/login">Login</a>
      <% } %>
    </nav>
  </div>
</header>

<main class="container">
